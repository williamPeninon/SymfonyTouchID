services:
    _defaults:
        autowire: true
        autoconfigure: true

    # Always available — even before user_class is wired.
    WpConsulting\PasskeyBundle\Installer\ProjectConfigurator: ~

    WpConsulting\PasskeyBundle\Command\PasskeyConfigureCommand:
        tags: ['console.command']
